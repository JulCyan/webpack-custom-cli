{
  "compilerOptions": {
    // 编译的目标是什么版本的
    "target": "esnext",
    // 指定生成哪个模块系统代码
    "module": "esnext",
    // 同时开启 alwaysStrict, noImplicitAny, noImplicitThis 和 strictNullChecks (2.3 以上)
    "strict": true,
    "importHelpers": true,
    "moduleResolution": "node",
    // 允许注解(装饰器)语法
    "experimentalDecorators": true,
    "esModuleInterop": true,
    // 允许引入没有默认导出的模块
    "allowSyntheticDefaultImports": true,
    // 生成对应的 map 文件
    "sourceMap": true,
    // 工作根目录
    "baseUrl": ".",
    "types": [
      "webpack-env"
    ],
    // 指定模块的路径，和 baseUrl 有关联，和webpack中 resolve.alias 配置一样
    "paths": {
      "@/*": [
        "src/*"
      ]
    },
    // 编译过程中需要引入的库文件的列表
    "lib": [
      "esnext",
      "dom",
      "dom.iterable",
      "scripthost"
    ]
  },
  // 指定一个匹配列表（属于自动指定该路径下的所有ts相关文件）
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "src/**/*.vue",
    "tests/**/*.ts",
    "tests/**/*.tsx"
  ],
  // 指定一个排除列表（include的反向操作）
  "exclude": [
    "node_modules"
  ]
}